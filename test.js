
var random = require('./')
  , assert = require('assert')

var last = null;

assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
assert('number' === typeof (last = random()));
